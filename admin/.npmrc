# Pack file: packages into node_modules instead of a symlink. A symlink to
# ../packages/contracts sits outside this app folder, so Next cannot resolve
# @mallanet/contracts without raising turbopack.root and nesting standalone.
install-links=true
