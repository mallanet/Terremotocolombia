# Pack file: packages into node_modules instead of a symlink. A symlink to
# ../packages/contracts sits outside turbopack.root (this app folder), so
# Next cannot resolve @mallanet/contracts and OpenNext cannot keep a flat
# standalone layout.
install-links=true
