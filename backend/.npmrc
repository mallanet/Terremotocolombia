# Pack file: packages into node_modules instead of a symlink. TypeScript does
# not walk from packages/contracts/src up into this package's node_modules to
# find zod when the dependency is a symlink (same pattern as drizzle-orm).
install-links=true
